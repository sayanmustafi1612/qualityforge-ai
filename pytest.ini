[pytest]
markers =
    ui: UI tests driven through Playwright
    api: API contract/schema tests
    integration: cross-cutting UI+API tests
    smoke: fast, high-value tests run on every PR
    regression: full regression suite, run on a schedule/nightly
testpaths = tests
addopts = -ra --strict-markers
